<h1>Transaction Details</h1>

    <p><strong>Description:</strong> <%= @transaction.description %></p>
    <p><strong>Amount:</strong> <%= br_currency(@transaction.amount) %></p>
    <p><strong>Category:</strong> <%= @transaction.category.name %></p>
    <p><strong>Date:</strong> <%= @transaction.created_at&.strftime('%B %d, %Y') || "Date not available" %></p>
    <p><strong>Payment Receipt:</strong></p>
        <% if @transaction.receipt.attached? %>
          <%= link_to 'Download Receipt', rails_blob_path(@transaction.receipt, disposition: "attachment"), class: "btn btn-link" %>
        <% else %>
          <p>No receipt attached.</p>
        <% end %>

<%= link_to 'Edit', edit_transaction_path(@transaction), class: 'btn btn-warning' %> |
<%= link_to 'Back', transactions_path, class: 'btn btn-secondary' %>
